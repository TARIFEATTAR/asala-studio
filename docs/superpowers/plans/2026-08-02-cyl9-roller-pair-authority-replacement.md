# CYL-9ML Roller Pair Authority Replacement Plan

> Execute inline with verification checkpoints. Keep the active release and Sanity unchanged.

1. Add failing tests for deterministic pair construction: plastic-owned alpha, metal-ball-only material transfer, single connected silhouette, exact alpha identity, calibrated placement, and opaque-white QA.
2. Implement the pair builder and emit a local inspection pack containing the two canonical layers, shared authority mask, evidence JSON, and five-body lineups.
3. Add failing UI policy tests proving revoked/failed candidates are audit-only and a clean replacement candidate can be reviewed even when its release parent mask is revoked.
4. Implement candidate selection and approval policy changes; preserve parent-mask blocking for ordinary queue actions.
5. Add a service-only staging command that uploads content-addressed sources/mask, creates two immutable manual candidate jobs against the existing PLASTIC/METAL component parents, and runs the deterministic worker for explicit job IDs.
6. Add the calibrated opaque-white-fraction QA row to METAL worker finalization.
7. Run focused tests, the full paper-doll suite, TypeScript, and the production build.
8. Generate and visually inspect the five-body plastic/metal lineup.
9. Stage both candidates in the linked Supabase project; verify their Storage SHAs, candidate ledger rows, QA rows, shared mask SHA, and reviewability in Studio.
10. Verify the active family release SHA/membership and Sanity publish ledger did not change. Commit and push the implementation to the existing draft PR.
